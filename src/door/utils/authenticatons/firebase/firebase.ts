import React from 'react';
import Firebase from 'react-native-firebase';
import {firebaseConfig} from '../index';

// initialize Firebase
Firebase.initializeApp(firebaseConfig, 'Needper')

const FirebaseAuth = () => {
    return;
};

export default FirebaseAuth;
